# 快速开始指南

本指南将帮助您在5分钟内启动并运行 RPA AI 自动化平台。

## 方式一：Docker 快速启动 (推荐)

### 前提条件
- Docker 20.10+
- Docker Compose 2.0+

### 步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd rpa-ai-platform
```

2. **配置环境变量**
```bash
# 复制示例配置
cp server/.env.example server/.env

# 编辑配置文件，至少需要配置以下项：
# - OPENAI_API_KEY (用于AI脚本生成)
```

3. **启动所有服务**
```bash
docker-compose up -d
```

4. **访问应用**
- 前端界面: http://localhost:3000
- 后端API: http://localhost:3001/api/v1
- 健康检查: http://localhost:3001/health

5. **查看日志**
```bash
docker-compose logs -f
```

## 方式二：本地开发启动

### 前提条件
- Node.js 18+
- PostgreSQL 15+
- Redis 7+

### 步骤

1. **安装依赖**
```bash
# 安装根依赖
npm install

# 安装服务端依赖
cd server && npm install

# 安装客户端依赖
cd ../client && npm install
```

2. **启动数据库服务**
```bash
# 使用Docker启动数据库（推荐）
docker-compose up -d postgres redis

# 或使用本地安装的数据库服务
```

3. **配置环境变量**
```bash
cd server
cp .env.example .env
# 编辑 .env 文件
```

4. **启动服务**
```bash
# 终端1: 启动后端
cd server
npm run dev

# 终端2: 启动前端
cd client
npm start
```

5. **访问应用**
- 前端: http://localhost:3000
- 后端: http://localhost:3001

## 第一个工作流

### 1. 创建工作流

1. 访问 http://localhost:3000
2. 点击侧边栏 "工作流" → "新建工作流"
3. 填写基本信息：
   - 名称: "百度搜索示例"
   - 描述: "自动打开百度并搜索关键词"

### 2. 配置步骤

点击"下一步"，添加以下步骤：

**步骤 1:**
- 操作类型: `navigate`
- 目标元素: `https://www.baidu.com`
- 描述: "打开百度首页"

**步骤 2:**
- 操作类型: `fill`
- 目标元素: `#kw`
- 值: `Playwright 自动化`
- 描述: "输入搜索关键词"

**步骤 3:**
- 操作类型: `click`
- 目标元素: `#su`
- 描述: "点击搜索按钮"

**步骤 4:**
- 操作类型: `wait`
- 目标元素: `#content_left`
- 描述: "等待搜索结果"

### 3. 上传截图（可选）

为每个步骤上传预期的截图，用于后续验证。

### 4. 生成脚本

1. 保存工作流
2. 在工作流列表中找到刚创建的工作流
3. 点击 "生成脚本" 按钮
4. 等待AI生成脚本（通常需要10-30秒）

### 5. 执行脚本

1. 转到 "脚本" 页面
2. 找到生成的脚本
3. 点击 "执行" 按钮
4. 在 "执行记录" 页面查看运行状态和日志

## 使用API

### 创建工作流

```bash
curl -X POST http://localhost:3001/api/v1/workflows \
  -H "Content-Type: application/json" \
  -d '{
    "name": "示例工作流",
    "steps": [
      {
        "order": 1,
        "action": "navigate",
        "target": "https://example.com"
      }
    ]
  }'
```

### 生成脚本

```bash
curl -X POST http://localhost:3001/api/v1/workflows/{workflow_id}/generate-script
```

### 执行脚本

```bash
curl -X POST http://localhost:3001/api/v1/executions \
  -H "Content-Type: application/json" \
  -d '{
    "scriptId": "{script_id}",
    "parameters": {}
  }'
```

## 常见操作类型

| 操作类型 | 说明 | 目标元素示例 | 值 |
|---------|------|-------------|-----|
| `navigate` | 导航到URL | `https://example.com` | - |
| `click` | 点击元素 | `button.submit`, `#login-btn` | - |
| `fill` | 填充输入框 | `input[name="username"]`, `#email` | `test@example.com` |
| `select` | 选择下拉选项 | `select#country` | `China` |
| `wait` | 等待元素出现 | `.loading`, `#content` | - |
| `screenshot` | 截取屏幕截图 | - | `screenshot.png` |

## CSS选择器示例

```css
# ID选择器
#username

# 类选择器
.btn-primary

# 标签选择器
button

# 属性选择器
input[type="email"]
button[data-action="submit"]

# 组合选择器
form .submit-button
div#login button.primary
```

## 故障排查

### 问题1: 无法生成脚本

**可能原因:**
- OpenAI API Key未配置或无效
- 网络连接问题

**解决方法:**
```bash
# 检查环境变量
echo $OPENAI_API_KEY

# 测试API连接
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"
```

### 问题2: 脚本执行失败

**可能原因:**
- 选择器不正确
- 网页加载速度慢
- 元素未找到

**解决方法:**
- 使用浏览器开发者工具验证选择器
- 增加等待时间
- 添加 `wait` 步骤确保元素已加载

### 问题3: Docker容器无法启动

**解决方法:**
```bash
# 查看日志
docker-compose logs

# 重新构建
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 下一步

- 查看 [开发指南](./DEVELOPMENT.md) 了解更多开发细节
- 查看 [API文档](./API.md) 了解完整的API接口
- 查看 [架构文档](./ARCHITECTURE.md) 了解系统架构

## 获取帮助

- GitHub Issues: 提交bug或功能请求
- 文档: 查看docs目录下的详细文档
- 示例: 查看examples目录下的示例代码

祝您使用愉快！🚀
